apiVersion: v1
kind: Service
metadata:
  labels:
    app: realestatebonusagent
    environment: Development
  name: realestatebonusagent
  namespace: services
spec:
  ports:
  - name: http
    port: 80
    protocol: TCP
    targetPort: 5000
  selector:
    app: realestatebonusagent
  sessionAffinity: None
  type: ClusterIP
